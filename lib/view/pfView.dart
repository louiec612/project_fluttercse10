import 'package:flutter/material.dart';
//VIEW FOR PROFILE
class profileView extends StatelessWidget {
  const profileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('pfView'),),
    );
  }
}
