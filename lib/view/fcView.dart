import 'package:flutter/material.dart';
//VIEW FOR FLASHCARD LISTS
class flashcardView extends StatelessWidget {
  const flashcardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('fcView'),),
    );
  }
}
